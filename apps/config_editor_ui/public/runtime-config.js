/*
 * Настройки контура для собранного фронтенда.
 *
 * Файл лежит рядом с index.html и правится без пересборки образа —
 * так адрес внутреннего API задаётся при развёртывании.
 *
 * apiBaseUrl === null  -> серверная интеграция выключена: редактор работает
 *                          полностью локально (импорт/экспорт файла).
 * apiBaseUrl === "..." -> адрес будущего внутреннего API (каталог таблиц,
 *                          проверка подключения, запуск, результаты).
 */
window.__DDL_BENCH_CONFIG__ = {
  apiBaseUrl: null,
};

import { Button, TextArea, TextField } from '@adqm/gpb-ui';
import type { SearchDimensions, AlternativeRule } from '../strategies/dimensions';

const lines = (value: string) => value.split('\n').map(item => item.trim()).filter(Boolean);

export function StrategyDimensionsEditor({ value, onChange }: { value: SearchDimensions; onChange(value: SearchDimensions): void }): JSX.Element {
  const set = <K extends keyof SearchDimensions>(key: K, next: SearchDimensions[K]) => onChange({ ...value, [key]: next });
  const alternatives = (key: 'column_types' | 'codecs', title: string, hint: string) => {
    const update = (index: number, patch: Partial<AlternativeRule>) => set(key, value[key].map((rule, i) => i === index ? { ...rule, ...patch } : rule));
    return <details className="strategy-dimension" open><summary>{title}<span>{value[key].length} правил</span></summary><div className="form-stack"><p>{hint} Каждая альтернатива — с новой строки.</p>{value[key].map((rule, index) => <div className="strategy-rule-editor" key={index}><div className="grid-2"><TextField label="Исходный тип колонки" value={rule.by_type} placeholder="Например, String" onChange={event => update(index, { by_type: event.target.value })} /><TextField label="Имя колонки (необязательно)" value={rule.by_name ?? ''} onChange={event => update(index, { by_name: event.target.value || undefined })} /></div><TextArea label={key === 'column_types' ? 'Альтернативные типы' : 'Кодеки и цепочки кодеков'} rows={3} value={rule.alternatives.join('\n')} onChange={event => update(index, { alternatives: event.target.value.split('\n') })} /><Button variant="secondary" onClick={() => set(key, value[key].filter((_, i) => i !== index))}>Удалить правило</Button></div>)}<Button variant="secondary" onClick={() => set(key, [...value[key], { by_type: '', alternatives: [] }])}>Добавить правило</Button></div></details>;
  };
  return <div className="form-stack strategy-dimensions">
    {alternatives('column_types', 'Типы колонок', 'Укажите, какие физические типы разрешено проверять для исходного типа.')}
    {alternatives('codecs', 'Кодеки', 'Настройте варианты сжатия отдельно от типов, например ZSTD(1) или Delta, ZSTD(3).')}
    <details className="strategy-dimension" open><summary>Skip-индексы<span>{value.skip_indexes.length} правил</span></summary><div className="form-stack"><p>Тип индекса может включать параметры. Гранулярность относится к индексу, а не к таблице.</p>{value.skip_indexes.map((rule, i) => {
      const update = (patch: Partial<typeof rule>) => set('skip_indexes', value.skip_indexes.map((r, j) => i === j ? { ...r, ...patch } : r));
      return <div className="strategy-rule-editor" key={i}><div className="grid-2"><TextField label="Исходный тип колонки" value={rule.by_type} onChange={event => update({ by_type: event.target.value })} /><TextField label="Имя колонки (необязательно)" value={rule.by_name ?? ''} onChange={event => update({ by_name: event.target.value || undefined })} /></div>{rule.indexes.map((index, j) => <div className="strategy-index-row" key={j}><TextField label="Тип индекса и параметры" value={index.type} placeholder="Например, bloom_filter(0.01)" onChange={event => update({ indexes: rule.indexes.map((r, k) => j === k ? { ...r, type: event.target.value } : r) })} /><TextField label="Гранулярность индекса" type="number" min={1} step={1} value={index.granularity || ''} onChange={event => update({ indexes: rule.indexes.map((r, k) => j === k ? { ...r, granularity: Number(event.target.value) } : r) })} /><Button variant="secondary" onClick={() => update({ indexes: rule.indexes.filter((_, k) => j !== k) })}>Убрать вариант</Button></div>)}<div className="inline-actions"><Button variant="secondary" onClick={() => update({ indexes: [...rule.indexes, { type: '', granularity: 1 }] })}>Добавить вариант индекса</Button><Button variant="secondary" onClick={() => set('skip_indexes', value.skip_indexes.filter((_, j) => i !== j))}>Удалить правило</Button></div></div>;
    })}<Button variant="secondary" onClick={() => set('skip_indexes', [...value.skip_indexes, { by_type: '', indexes: [] }])}>Добавить правило индекса</Button></div></details>
    <details className="strategy-dimension" open><summary>ORDER BY<span>Ключ сортировки</span></summary><div className="form-stack"><TextField label="Первая колонка (необязательно)" value={value.order_by.first_column ?? ''} onChange={event => set('order_by', { ...value.order_by, first_column: event.target.value || undefined })} /><TextArea label="Кандидаты ORDER BY — по одному на строку" rows={3} value={value.order_by.candidates.join('\n')} onChange={event => set('order_by', { ...value.order_by, candidates: event.target.value.split('\n') })} /></div></details>
    <details className="strategy-dimension" open><summary>Порядок перебора колонок<span>Не физический ORDER BY</span></summary><div className="form-stack"><p>Задайте уникальные позиции, определяющие только очерёдность исследования колонок.</p>{value.column_order.map((row, i) => <div className="strategy-index-row" key={i}><TextField label="Колонка" value={row.column} onChange={event => set('column_order', value.column_order.map((r, j) => i === j ? { ...r, column: event.target.value } : r))} /><TextField label="Позиция перебора" type="number" min={1} step={1} value={row.position || ''} onChange={event => set('column_order', value.column_order.map((r, j) => i === j ? { ...r, position: Number(event.target.value) } : r))} /><Button variant="secondary" onClick={() => set('column_order', value.column_order.filter((_, j) => i !== j))}>Удалить</Button></div>)}<Button variant="secondary" onClick={() => set('column_order', [...value.column_order, { column: '', position: value.column_order.length + 1 }])}>Добавить колонку</Button></div></details>
    <details className="strategy-dimension" open><summary>Гранулярность таблицы<span>index_granularity</span></summary><div className="form-stack"><p>Число строк в грануле таблицы. Добавьте каждый проверяемый вариант отдельно.</p>{value.table_index_granularity_values.map((n, i) => <div className="strategy-index-row" key={i}><TextField label="Строк в грануле" type="number" step={1} min={1} value={n || ''} onChange={event => set('table_index_granularity_values', value.table_index_granularity_values.map((v, j) => i === j ? Number(event.target.value) : v))} /><Button variant="secondary" onClick={() => set('table_index_granularity_values', value.table_index_granularity_values.filter((_, j) => i !== j))}>Удалить</Button></div>)}<Button variant="secondary" onClick={() => set('table_index_granularity_values', [...value.table_index_granularity_values, 8192])}>Добавить вариант</Button></div></details>
  </div>;
}

export function normalizeDimensions(value: SearchDimensions): SearchDimensions {
  return { ...value, column_types: value.column_types.map(rule => ({ ...rule, alternatives: lines(rule.alternatives.join('\n')) })), codecs: value.codecs.map(rule => ({ ...rule, alternatives: lines(rule.alternatives.join('\n')) })), order_by: { ...value.order_by, candidates: lines(value.order_by.candidates.join('\n')) } };
}

import { Button, TextField } from '@adqm/gpb-ui';
import type { ReactNode } from 'react';
import { AlternativeChipInput } from './AlternativeChipInput';
import { SourceTypeAutocomplete } from './SourceTypeAutocomplete';
import { MaterialIcon, type MaterialIconName } from './MaterialIcon';
import type { AlternativeRule, SearchDimensions } from '../strategies/dimensions';
import { useI18n } from '../i18n';

type Accent = 'types' | 'codecs' | 'indexes' | 'order' | 'column-order' | 'granularity';

function Dimension({ title, status, hint, accent, icon, open = false, children }: { title: string; status: string; hint: string; accent: Accent; icon: MaterialIconName; open?: boolean; children: ReactNode }): JSX.Element {
  return <details className={`strategy-dimension strategy-dimension-${accent}`} open={open}><summary><span className="strategy-dimension-title"><i><MaterialIcon name={icon} size={18} /></i>{title}</span><span className="strategy-dimension-status">{status}</span></summary><div className="form-stack"><p>{hint}</p>{children}</div></details>;
}

export function StrategyDimensionsEditor({ value, onChange }: { value: SearchDimensions; onChange(value: SearchDimensions): void }): JSX.Element {
  const { t } = useI18n();
  const set = <K extends keyof SearchDimensions>(key: K, next: SearchDimensions[K]) => onChange({ ...value, [key]: next });
  const ruleStatus = (rules: AlternativeRule[]) => rules.length ? t(`${rules.length} правил`, `${rules.length} rules`) : t('Не настроено', 'Not configured');
  const alternatives = (key: 'column_types' | 'codecs', title: string, hint: string, accent: 'types' | 'codecs', icon: 'data' | 'tune') => <Dimension title={title} status={ruleStatus(value[key])} hint={hint} accent={accent} icon={icon} open={key === 'column_types'}>
    {value[key].map((rule, index) => <div className="strategy-rule-editor" key={`${key}-${index}`}><header><strong>{t('Правило', 'Rule')} {index + 1}</strong><Button variant="tertiary" className="strategy-remove" onClick={() => set(key, value[key].filter((_, itemIndex) => itemIndex !== index))}>{t('Удалить правило', 'Remove rule')}</Button></header><div className="grid-2"><SourceTypeAutocomplete value={rule.by_type} onChange={(by_type) => set(key, value[key].map((item, itemIndex) => itemIndex === index ? { ...item, by_type } : item))} /><TextField label={t('Имя колонки (необязательно)', 'Column name (optional)')} value={rule.by_name ?? ''} onChange={event => set(key, value[key].map((item, itemIndex) => itemIndex === index ? { ...item, by_name: event.target.value || undefined } : item))} /></div><AlternativeChipInput kind={key === 'column_types' ? 'column_type' : 'codec_alternative'} label={key === 'column_types' ? t('Альтернативные типы', 'Alternative types') : t('Кодеки и цепочки', 'Codecs and chains')} matcherType={rule.by_type} values={rule.alternatives} onChange={(alternatives) => set(key, value[key].map((item, itemIndex) => itemIndex === index ? { ...item, alternatives } : item))} placeholder={key === 'column_types' ? 'LowCardinality(String)' : 'ZSTD(3)'} tone={accent} /></div>)}
    <Button variant="secondary" className="strategy-add" onClick={() => set(key, [...value[key], { by_type: '', alternatives: [] }])}>＋ {t('Добавить правило', 'Add rule')}</Button>
  </Dimension>;
  return <div className="form-stack strategy-dimensions">
    {alternatives('column_types', t('Типы колонок', 'Column types'), t('Укажите разрешённые физические типы. Добавляйте варианты из списка или вручную.', 'Define allowed physical types. Add alternatives from the list or manually.'), 'types', 'data')}
    {alternatives('codecs', t('Кодеки', 'Codecs'), t('Каждый чип — один полный вариант или цепочка сжатия.', 'Each chip is one complete alternative or compression chain.'), 'codecs', 'tune')}
    <Dimension title={t('Skip-индексы', 'Skip indexes')} status={value.skip_indexes.length ? t(`${value.skip_indexes.length} правил`, `${value.skip_indexes.length} rules`) : t('Не настроено', 'Not configured')} hint={t('Тип и гранулярность остаются частью одного варианта.', 'Type and granularity remain part of one alternative.')} accent="indexes" icon="strategy">
      {value.skip_indexes.map((rule, index) => <div className="strategy-rule-editor" key={`indexes-${index}`}><header><strong>{t('Правило', 'Rule')} {index + 1}</strong><Button variant="tertiary" className="strategy-remove" onClick={() => set('skip_indexes', value.skip_indexes.filter((_, itemIndex) => itemIndex !== index))}>{t('Удалить правило', 'Remove rule')}</Button></header><div className="grid-2"><SourceTypeAutocomplete value={rule.by_type} onChange={(by_type) => set('skip_indexes', value.skip_indexes.map((item, itemIndex) => itemIndex === index ? { ...item, by_type } : item))} /><TextField label={t('Имя колонки (необязательно)', 'Column name (optional)')} value={rule.by_name ?? ''} onChange={event => set('skip_indexes', value.skip_indexes.map((item, itemIndex) => itemIndex === index ? { ...item, by_name: event.target.value || undefined } : item))} /></div>{rule.indexes.map((item, itemIndex) => <div className="strategy-index-row" key={`${item.type}-${itemIndex}`}><TextField label={t('Тип индекса', 'Index type')} value={item.type} onChange={event => set('skip_indexes', value.skip_indexes.map((current, currentIndex) => currentIndex === index ? { ...current, indexes: current.indexes.map((entry, entryIndex) => entryIndex === itemIndex ? { ...entry, type: event.target.value } : entry) } : current))} /><TextField label={t('Гранулярность', 'Granularity')} type="number" min={1} step={1} value={item.granularity} onChange={event => set('skip_indexes', value.skip_indexes.map((current, currentIndex) => currentIndex === index ? { ...current, indexes: current.indexes.map((entry, entryIndex) => entryIndex === itemIndex ? { ...entry, granularity: Number(event.target.value) } : entry) } : current))} /><Button variant="tertiary" className="strategy-remove" onClick={() => set('skip_indexes', value.skip_indexes.map((current, currentIndex) => currentIndex === index ? { ...current, indexes: current.indexes.filter((_, entryIndex) => entryIndex !== itemIndex) } : current))}>{t('Удалить', 'Remove')}</Button></div>)}<AlternativeChipInput kind="skip_index" label={t('Добавить индекс', 'Add index')} matcherType={rule.by_type} values={rule.indexes.map((item) => item.type)} onChange={(types) => set('skip_indexes', value.skip_indexes.map((current, currentIndex) => currentIndex === index ? { ...current, indexes: types.map((type) => current.indexes.find((item) => item.type === type) ?? { type, granularity: 4 }) } : current))} placeholder="bloom_filter(0.01)" tone="indexes" /></div>)}
      <Button variant="secondary" className="strategy-add" onClick={() => set('skip_indexes', [...value.skip_indexes, { by_type: '', indexes: [] }])}>＋ {t('Добавить правило индекса', 'Add index rule')}</Button>
    </Dimension>
    <Dimension title="ORDER BY" status={value.order_by.candidates.length ? t(`${value.order_by.candidates.length} вариантов`, `${value.order_by.candidates.length} alternatives`) : t('Не настроено', 'Not configured')} hint={t('Каждый ключ сортировки — отдельный вариант. Не разделяйте выражения запятыми.', 'Each sorting key is a separate alternative. Do not split expressions by commas.')} accent="order" icon="benchmark"><TextField label={t('Первая колонка (необязательно)', 'First column (optional)')} value={value.order_by.first_column ?? ''} onChange={event => set('order_by', { ...value.order_by, first_column: event.target.value || undefined })} /><AlternativeChipInput kind="source_type" label={t('Кандидаты ORDER BY', 'ORDER BY candidates')} matcherType="" values={value.order_by.candidates} onChange={(candidates) => set('order_by', { ...value.order_by, candidates })} placeholder={t('Введите выражение вручную', 'Enter an expression manually')} tone="types" showSuggestions={false} /></Dimension>
    <Dimension title={t('Порядок колонок', 'Column order')} status={value.column_order.length ? t(`${value.column_order.length} позиций`, `${value.column_order.length} positions`) : t('Не настроено', 'Not configured')} hint={t('Укажите отдельные позиции для колонок, которые нужно переставлять. Повторы не допускаются.', 'Set separate positions for columns that may be reordered. Duplicates are not allowed.')} accent="column-order" icon="source">
      {value.column_order.map((item, index) => <div className="strategy-index-row" key={`${item.column}-${index}`}><TextField label={t('Колонка', 'Column')} value={item.column} placeholder="event_time" onChange={event => set('column_order', value.column_order.map((row, rowIndex) => rowIndex === index ? { ...row, column: event.target.value } : row))} /><TextField label={t('Позиция', 'Position')} type="number" min={1} step={1} value={item.position} onChange={event => set('column_order', value.column_order.map((row, rowIndex) => rowIndex === index ? { ...row, position: Number(event.target.value) } : row))} /><Button variant="tertiary" className="strategy-remove" onClick={() => set('column_order', value.column_order.filter((_, rowIndex) => rowIndex !== index))}>{t('Удалить', 'Remove')}</Button></div>)}
      <Button variant="secondary" className="strategy-add" onClick={() => set('column_order', [...value.column_order, { column: '', position: value.column_order.length + 1 }])}>＋ {t('Добавить позицию', 'Add position')}</Button>
    </Dimension>
    <Dimension title={t('Гранулярность таблицы', 'Table granularity')} status={value.table_index_granularity_values.length ? t(`${value.table_index_granularity_values.length} значений`, `${value.table_index_granularity_values.length} values`) : t('Не настроено', 'Not configured')} hint={t('Число строк в грануле таблицы. Это не гранулярность skip-индекса.', 'Rows per table granule. This is not skip-index granularity.')} accent="granularity" icon="analytics"><AlternativeChipInput kind="table_granularity" label={t('Варианты гранулярности', 'Granularity alternatives')} matcherType="" values={value.table_index_granularity_values.map(String)} onChange={(values) => set('table_index_granularity_values', values.map(Number).filter((item) => Number.isInteger(item) && item > 0))} placeholder="8192" tone="granularity" /></Dimension>
  </div>;
}

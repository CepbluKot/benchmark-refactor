# Load definitions on startup so bench vhost starts with empty policies.
management.load_definitions = /etc/rabbitmq/definitions.json

# Keep default loopback-only access for guest user.
loopback_users.guest = true
